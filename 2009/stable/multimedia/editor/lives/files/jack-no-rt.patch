Index: lives-1.0.9/src/main.c
===================================================================
--- lives-1.0.9.orig/src/main.c
+++ lives-1.0.9/src/main.c
@@ -1029,7 +1029,7 @@ static void lives_init(_ign_opts *ign_op
 	get_location("jackd",jackd_loc,512);
 	if (strlen(jackd_loc)) {
 #ifndef IS_DARWIN
-	  com=g_strdup_printf("echo \"%s -Z -d alsa -s\">%s",jackd_loc,prefs->jack_aserver);
+	  com=g_strdup_printf("echo \"%s -r -Z -d alsa -s\">%s",jackd_loc,prefs->jack_aserver);
 #else
 	  // use coreaudio on Darwin
 	  com=g_strdup_printf("echo \"%s -Z -d coreaudio -s\">%s",jackd_loc,prefs->jack_aserver);
