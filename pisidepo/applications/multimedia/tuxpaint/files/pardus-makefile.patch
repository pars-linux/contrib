diff -Nur tuxpaint-0.9.15b.orig/Makefile tuxpaint-0.9.15b/Makefile
--- Makefile	2005-11-27 08:44:15.000000000 +0200
+++ Makefile	2006-09-23 01:40:21.000000000 +0300
@@ -17,7 +17,7 @@
 
 # Where to install things:
 
-PREFIX=/usr/local
+PREFIX=/usr
 
 
 # Root directory to place files when creating packages.
@@ -61,13 +61,14 @@
 
 ICON_PREFIX=$(PKG_ROOT)$(PREFIX)/share/pixmaps
 X11_ICON_PREFIX=$(PKG_ROOT)$(PREFIX)/X11R6/include/X11/pixmaps
-GNOME_PREFIX=`gnome-config --prefix`
-KDE_PREFIX=`kde-config --install apps --expandvars`
-KDE_ICON_PREFIX=`kde-config --install icon --expandvars`
+GNOME_PREFIX=
+KDE_PREFIX=
+KDE_ICON_PREFIX=
 
 
 # Locale files
 
+ENABLE_GETTEXT=1
 LOCALE_PREFIX=$(PKG_ROOT)$(PREFIX)/share/locale
 # LOCALE_PREFIX=/usr/share/locale
 
@@ -95,7 +96,7 @@
 
 # The entire set of CFLAGS:
 
-CFLAGS=-O2 -W -Wall -fno-common -ffast-math \
+CFLAGS = -W -Wall -fno-common \
 	-Wcast-align -Wredundant-decls \
 	-Wbad-function-cast -Wwrite-strings \
 	-Waggregate-return \
@@ -552,9 +553,6 @@
 	@install -d $(ICON_PREFIX)
 	@cp data/images/icon.png $(ICON_PREFIX)/tuxpaint.png
 	@chmod 644 $(ICON_PREFIX)/tuxpaint.png
-	@install -d $(X11_ICON_PREFIX)
-	@cp data/images/icon32x32.xpm $(X11_ICON_PREFIX)/tuxpaint.xpm
-	@chmod 644 $(X11_ICON_PREFIX)/tuxpaint.xpm
 
 
 # Install the program:
@@ -933,7 +931,7 @@
 	@$(CC) $(CFLAGS) $(DEBUG_FLAGS) $(SDL_CFLAGS) $(DEFS) \
 		-o tuxpaint obj/tuxpaint.o $(HQXX_O) \
 		$(ARCH_LIBS) $(SDL_LIBS) \
-		-lm $(ARCH_LINKS)
+		-lm -lpng12 $(ARCH_LINKS)
 	@$(RSRC_CMD)
 	@$(MIMESET_CMD)
 
diff -Nur tuxpaint-0.9.15b.orig/src/tuxpaint.desktop tuxpaint-0.9.15b/src/tuxpaint.desktop
--- src/tuxpaint.desktop	2005-10-11 08:11:38.000000000 +0300
+++ src/tuxpaint.desktop	2006-09-23 01:43:24.000000000 +0300
@@ -39,7 +39,7 @@
 Exec=tuxpaint
 Icon=tuxpaint
 Terminal=false
-Categories=Education;RasterGraphics;Graphics;Art;
+Categories=Application;Graphics;Art;
 GenericName=Drawing program
 GenericName[af]=Tekenprogram
 GenericName[be]=Праграма для маляваньня
