--- configure.orig	2007-02-23 16:13:28.000000000 +0200
+++ configure	2007-02-27 01:19:59.000000000 +0200
@@ -6299,10 +6299,7 @@
 
 { echo "$as_me:$LINENO: checking for xmms_show_message in -laudacious" >&5
 echo $ECHO_N "checking for xmms_show_message in -laudacious... $ECHO_C" >&6; }
-if test "${ac_cv_lib_audacious_xmms_show_message+set}" = set; then
-  echo $ECHO_N "(cached) $ECHO_C" >&6
-else
-  ac_check_lib_save_LIBS=$LIBS
+ac_check_lib_save_LIBS=$LIBS
 LIBS="-laudacious  $LIBS"
 cat >conftest.$ac_ext <<_ACEOF
 /* confdefs.h.  */
@@ -6380,18 +6377,10 @@
 _ACEOF
 
   LIBS="-laudacious $LIBS"
-
-else
-  { { echo "$as_me:$LINENO: error: *** Unable to find Audacious!" >&5
-echo "$as_me: error: *** Unable to find Audacious!" >&2;}
-   { (exit 1); exit 1; }; }
 fi
 
   audacious_effect_plugin_dir=`pkg-config audacious --variable=effect_plugin_dir`
 
-
-fi
-
 if test "x$cv_need_bmp" = "xyes"; then
 
 pkg_failed=no
